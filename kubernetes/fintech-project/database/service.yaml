# database/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: fintech
spec:
  selector:
    app: postgres
    tier: database
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP  # ✅ Fixed!