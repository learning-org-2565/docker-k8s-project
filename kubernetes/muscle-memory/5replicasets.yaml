apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: arun-replicaset
  labels:
    app: nginx-replica
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
      tier: backend
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
        - name: nginx
          image: nginx:1.25
          ports:
          - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: service-replica
spec:
  selector:
    app: nginx
  ports:
  - port: 80
    targetPort: 80