apiVersion: v1
kind: Service
metadata:
  name: payment-api-service
  labels:
    purpose: payment-process
spec:
  type: ClusterIP
  selector:
    purpose: payment-process
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
