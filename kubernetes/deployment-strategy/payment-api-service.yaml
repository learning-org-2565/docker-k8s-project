apiVersion: v1
kind: Service
metadata:
  name: payment-api-service
  labels:
      app: payment-chaos
spec:
  type: ClusterIP
  selector:
    app: payment-chaos
  ports:  
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
